{
  "name": "signal-tree",
  "version": "0.1.0",
  "description": "A JavaScript execution controller",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib && rimraf tests/*.js*",
    "prebuild": "typings install",
    "build": "tsc && tsc --outDir tests tests/createSignal.spec.ts --sourceMap typings/main.d.ts",
    "pretest": "npm run build",
    "test": "node tests/createSignal.spec.js",
    "prepublish": "npm run build",
    "watch": "tsc -w"
  },
  "author": "Alexey Gurianov <gurianov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.5.2",
    "ts-node": "^0.7.2",
    "typescript": "^1.9.0-dev.20160429",
    "typings": "^0.8.1"
  },
  "dependencies": {
    "object-assign": "^4.0.1"
  },
  "files": [
    "lib"
  ]
}
