{
  "name": "signal-tree",
  "version": "0.1.0",
  "description": "A JavaScript execution controller",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib && rimraf tests/*.js*",
    "prebuild": "npm run clean && typings install",
    "build": "tsc",
    "build:tests": "tsc --lib es5,dom,es2015.promise --sourceMap --outDir tests tests/index.spec.ts typings/main.d.ts",
    "pretest": "npm run build && npm run build:tests",
    "test": "node tests/index.spec.js",
    "prepublish": "npm run build",
    "watch": "tsc -w"
  },
  "author": "Alexey Gurianov <gurianov@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.5.2",
    "ts-node": "^0.7.2",
    "typescript": "^1.9.0-dev.20160429",
    "typings": "^0.8.1"
  },
  "dependencies": {
    "object-assign": "^4.0.1"
  },
  "files": [
    "lib"
  ]
}
